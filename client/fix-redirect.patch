--- a/create-campaign.html
+++ b/create-campaign.html
@@ -1111,7 +1111,20 @@
       });
       clearTimeout(tid);
-      const vData = await vRes.json();
+      
+      // Check HTTP status before parsing JSON to prevent redirects
+      if (!vRes.ok) {
+        if (vRes.status === 401) {
+          toast('⚠️ Session expired. Please login again.', 4000);
+          setTimeout(() => window.location.href = 'login.html', 2000);
+          return;
+        } else if (vRes.status >= 400 && vRes.status < 500) {
+          toast('❌ Request error: ' + vRes.status, 4000);
+          $('p1b').className = 'pbadge err'; $('p1b').textContent = 'Failed';
+          btn.disabled = false; btn.innerHTML = '🔄 Retry'; return;
+        } else if (vRes.status >= 500) {
+          toast('❌ Server error. Please try again.', 4000);
+          $('p1b').className = 'pbadge err'; $('p1b').textContent = 'Failed';
+          btn.disabled = false; btn.innerHTML = '🔄 Retry'; return;
+        }
+      }
+      
+      const vData = await vRes.json();
       console.log('📥 Preview result:', vData);
